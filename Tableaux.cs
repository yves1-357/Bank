// Calculer de la somme et de la moyenne des éléments d'un tableau
/*
int [] nombreTableau = {10, 20, 30};
int somme = 0;
for (int i = 0; i < nombreTableau.Length; i++) 
{
    somme += nombreTableau[i];
}
Console.WriteLine($"la somme vaut {somme}");

*/
//Trouver les éléments maximum et minimum dans un tableau

/*
int [] nombreTableau = {10, 20, 30, 50, 70};
double min = nombreTableau[0];
double max = nombreTableau[0];
for (int i = 0; i < nombreTableau.Length; i++)
{
    if (nombreTableau[i] < min)
    {
        min = nombreTableau[i];
    }

    if (nombreTableau[i] > max)
    {
        max = nombreTableau[i];
    }
}

Console.WriteLine($"le nombre min est : {min}");
    Console.WriteLine($"le nombre max est: {max}");

*/

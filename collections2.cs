//Grâce à une boucle “while” et à l’aide d’une collection, calculez les nombres
//premiers inférieur à un nombre entier entré au clavier.

using System.Net;
/*
Console.WriteLine(" Entrez un  nombre ");
int nombreUtilisateur = int.Parse(Console.ReadLine());
int nombreTests = 2;
List<int> nombrePremiers = new List<int>();

while (nombreTests < nombreUtilisateur) 
{
   if (EstnombrePremier(nombreTests))
   {
      nombrePremiers.Add(nombreTests);
   }

   nombreTests++;
   
}
Console.WriteLine("nombre 1er trouve: " +string.Join(",", nombrePremiers));


bool EstnombrePremier(int nombre)
{
   if (nombre <= 1) return false;
   for (int i = 2; i < Math.Sqrt(nombre); i++)
   {
      if (nombre % i == 0) return false;
   }

   return true;
}

*/